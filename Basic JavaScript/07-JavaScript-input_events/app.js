// input events

const filter = document.getElementById("filter");

// document.addEventListener("DOMContentLoaded", load);

// function load(event) {
//     console.log("sayfa yüklendi");
// }

// filter.addEventListener("focus", run);
// filter.addEventListener("blur", run);

// function run(event) {
//     console.log(event.type);
// }

// filter.addEventListener("paste", run);  // yapıştırma yapınca bu event oluşur
// filter.addEventListener("copy", run); // kopyalama yapınca bu event oluşur
// filter.addEventListener("cut", run); // kesme yapınca bu event oluşur
// filter.addEventListener("select", run); // seçme yapınca bu event oluşur